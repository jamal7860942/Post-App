<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="flex justify-end">
            <a class="text-sm bg-red-500 px-3 py-2 rounded-md mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <h5 class="text-xl">Edit your post.</h5>
        <form action="/update/<%=post._id%>" method="post">
            <textarea name="content" placeholder="What's in your Mind?" class="p-3 mt-2 w-1/3 bg-transparent border-2 border-zinc-800 outline-none rounded-md resize-none"><%=post.content%></textarea>
            <input class="px-5 py-2 w-40 rounded-md mt-2 cursor-pointer block bg-[#7FB069]" type="submit" value="Update Post">
        </form>
    </div>
</body>

</html>