<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
      window.tailwind = {
        theme: {
          extend: {
            // This tells Tailwind to enable backdrop-filter utilities
            backdropFilter: {
              'none': 'none',
              'sm': 'blur(4px)',
              'md': 'blur(12px)',
              'lg': 'blur(16px)',
              'xl': 'blur(24px)',
            },
          },
        },
      }
    </script>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main-div w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="flex justify-end">
            <a class="text-sm bg-red-500 px-3 py-2 rounded-md mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="h-12 w-12 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%=user.profilepic%>" alt="">
            </div>
            <h3 class="text-5xl tracking-tighter mb-5"><span class="font-light">Hello, <span class="capitalize"><%=user.name%></span></span></h3>
        </div>
        <h5 class="text-xl">You can create a new post.</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="What's in your Mind?" class="p-3 mt-2 w-1/3 bg-transparent border-2 border-zinc-800 outline-none rounded-md resize-none"></textarea>
            <input class="px-5 py-2 w-40 rounded-md mt-2 cursor-pointer block bg-blue-500" type="submit" value="Create New Post">
        </form>

        <div class="posts mt-32">
            <h3 class="text-zinc-400 text-xl">Your Posts</h3>
            <div class="postcontainer mt-5 flex gap-5 flex-wrap">
                <% if(user.posts.length > 0){%>
                    <% user.posts.reverse().forEach(post =>{%>
                        <div class="post mb-4 w-1/3 p-4 rounded-md border-[1px] border-zinc-700 bg-zinc-800">
                    <h4 class="mb-2 text-blue-500">@<%=user.username%></h4>
                    <p class="text-sm tracking-tight"><%=post.content%></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %> Like</small>
                    <div class="btns flex mt-1 gap-5">
                        <a href="/like/<%=post._id%>" class="text-blue-500"><%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %></a>
                        <a href="/edit/<%=post._id%>" class="text-zinc-400">Edit</a>
                        <a href="/delete/<%=post._id%>" class="text-red-500 openModal delete-link">Delete</a>
                    </div>
                </div>
                        <%}) %>
                    <%}
                    else {%>
                        <div class="text-zinc-600">No Post Yet!</div>
                        <%}
                 %>
            </div>
        </div>
    </div>
    <div class="deleteModal hidden fixed inset-0 w-full min-h-screen bg-black bg-opacity-90 backdrop-blur-sm flex justify-center items-center z-50">
    
    <div class="modal h-auto p-10 shadow-lg rounded-md
                bg-gray-800 border border-gray-700">
        
        <h3 class="text-white text-lg font-semibold mb-4">Are you sure?</h3>
        
        <div class="flex gap-5 mt-3">
            <button class="del-btn bg-red-500 text-white px-5 py-2 rounded-md font-semibold">Delete</button>
            <button class="cancel-btn bg-gray-300 text-gray-800 px-5 py-2 rounded-md font-semibold">Cancel</button>
        </div>
    </div>
</div>
</div>
    <script type="module" src="/javascripts/popup.js"></script>
</body>

</html>